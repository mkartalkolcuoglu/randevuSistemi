{
  "buildCommand": "cd apps/admin && npx prisma generate && cd ../.. && npx turbo run build --filter=admin",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  }
}

